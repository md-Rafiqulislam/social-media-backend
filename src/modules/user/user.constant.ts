
// user role
export enum userRole {
    user = 'user',
    admin = 'admin',
    superAdmin = 'superAdmin',
};

// user gender
export enum userGender {
    male = 'male',
    female = 'female',
    other = 'other',
};

// user status
export enum userStatus {
    active = 'active',
    blocked = 'blocked',
};